// Compose compiler stability configuration.
//
// Types listed here are treated as stable even though the compiler cannot infer it,
// either because they live in a module that is not compiled with the Compose compiler
// (`:core`) or because they come from the standard library.
//
// Everything listed below is effectively immutable after construction.

// Core model types: BackgroundShape, BackgroundItemStyle, ImageSource, MergeType,
// TitleGravity, CalendarDate, DayOfWeekConstants and the date-time providers.
com.mamboa.yearview.core.*

// Locale instances are immutable and are only ever read.
java.util.Locale
